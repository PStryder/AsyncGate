"""AsyncGate MCP surface.

Served by `http.py` as hand-rolled JSON-RPC. `create_mcp_server` used to be
re-exported here; it was dead code against an API the installed mcp package
does not have. See the note in server.py.
"""
